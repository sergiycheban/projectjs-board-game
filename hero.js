var Hero = function(name, attack, armor, HP, impactRadius, speed) {
  (this.name = name),
    (this.attack = attack),
    (this.armor = armor),
    (this.HP = HP),
    (this.impactRadius = impactRadius),
    (this.speed = speed);
};
